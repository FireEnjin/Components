import { ComponentPresets } from "@fireenjin/docs";

export default {
  default: {
    name: "default",
    props: {
      options: [
        {
          label: "Option 1",
          value: "option1",
        },
      ],
    },
  },
} as ComponentPresets;

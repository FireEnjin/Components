import { ComponentPresets } from "@fireenjin/docs";

export default {
  default: {
    props: {
      value: {
        test: {
          asdf: true,
        },
      },
    },
  },
} as ComponentPresets;

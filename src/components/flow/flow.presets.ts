import { ComponentPresets } from "@fireenjin/docs";

export default {
  default: {
    name: "Default",
    props: {
      fields: [
        {
          label: "WEeee",
        },
        {
          label: "Twee",
        },
      ],
    },
  },
} as ComponentPresets;

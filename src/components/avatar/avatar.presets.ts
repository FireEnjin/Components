import { OrganismPresets } from "@fireenjin/docs/dist/types/interfaces";

export default {
  default: {
    name: "Default",
    props: {
      src: null,
      initials: "TT",
    },
  },
} as OrganismPresets;

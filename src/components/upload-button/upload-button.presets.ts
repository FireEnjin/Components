import { ComponentPresets } from "@fireenjin/docs";

export default {
  default: {
    props: {
      placeholder: "Upload File",
      showLink: true,
      showClear: true,
    },
  },
} as ComponentPresets;
